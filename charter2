#include <iostream>
using namespace std;
int main()
{
	int a = 0;
	float b = 3.14f;
	char c = 'a';
	char e[] = "string";
	bool d = false;

	
	cout << "请给整型变量a赋值：" << endl;
	cin >> a;
	cout << "整型变量a等于" << a << endl;

	cout << "请给浮点型变量b赋值：" << endl;
	cin >> b;
	cout << "浮点型变量b等于" << b << endl;

	cout << "请给字符型变量c赋值：" << endl;
	cin >> c;
	cout << "字符型变量c等于" << c << endl;

		system("pause");
		return 0;
}

输入
1 整型
int a = 0;


运算符
作用：用于执行代码的运算
 运算符类型					作用
算术运算符				用于处理四则运算
赋值运算符				用于将表达式的值赋给变量
比较运算符				用于表达式的比较，并返回一个真值或假值
逻辑运算符				用于根据表达式的值返回真值或假值

算术运算符
加减乘除  %取模（取余）
++递增运算符
1 前置递增  ++a让变量a+1操作		先让变量+1再运算  先+后算
2 后置递增  b++让变量b+1			先让变量运算再+1  先算后+

--递减运算符

赋值运算符
作用：用于将表达式的值赋值给变量
=   +=   -=   *=    /=   %=
